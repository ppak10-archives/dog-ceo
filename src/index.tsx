/**
 * index.tsx
 * Entry file for app.
 */

// Node Modules
import React from 'react';
import {render} from 'react-dom';

// Components
import App from './App';

render(<App />, document.getElementById('root'));
